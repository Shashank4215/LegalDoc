# Legal Case Management System - Python Dependencies

# Database (PostgreSQL)
psycopg2-binary>=2.9.9

# Database (MongoDB)
pymongo>=4.6.0

# AI/ML
anthropic>=0.34.0

# Vector Embeddings & Similarity
transformers>=4.30.0  # For Arabic BERT model
torch>=2.0.0  # PyTorch for transformers
faiss-cpu>=1.7.4  # FAISS for fast similarity search (CPU version)
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Document Processing
python-docx>=1.1.0
pdfplumber>=0.10.0
python-dateutil>=2.8.2

# Arabic Text Processing
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
regex>=2023.10.3
jellyfish>=1.0.0

# Optional: Arabic NLP (if needed)
# camel-tools>=1.5.0

# Web Framework (for API)
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Utilities
python-dotenv>=1.0.0

# LangGraph and LangChain for Query Agent
langgraph>=0.2.0
langchain>=0.3.0
langchain-groq>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Local LLM Support (vLLM for fast inference)
vllm>=0.6.0  # High-performance inference engine
huggingface-hub>=0.20.0  # For downloading models
accelerate>=0.27.0  # For model loading
bitsandbytes>=0.41.0  # Optional: for quantization if needed

# Monitoring and logging
psutil>=5.9.0  # For system monitoring

