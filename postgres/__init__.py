"""
PostgreSQL-specific modules for the legacy / v2 JSONB + FAISS architecture.

This package isolates all code that depends on psycopg2 / PostgreSQL so the
MongoDB-based pipeline can stay clean.
"""


